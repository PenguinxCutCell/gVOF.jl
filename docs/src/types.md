```@meta
CurrentModule = gVOF
```

# Types

`VOFParams` stores runtime controls (reconstruction/advection methods, tolerances,
grid dimensions, and time-stepping controls).

`VOFGrid` stores the geometry, topology, and all VOF state arrays used by the
algorithms.

See [Reference](@ref reference) for full API docstrings.
